version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm install --legacy-peer-deps
      - npm run build
run:
  runtime-version: 22.17.0
  command: npm start
  network:
    port: 3001
    env: PORT
  env:
    - name: NODE_ENV
      value: production
    - name: AWS_REGION
      value: ap-southeast-3
    - name: POSTGRES_HOST
      value: epai-db.cj0wcmsam8bg.ap-southeast-3.rds.amazonaws.com
    - name: POSTGRES_PORT
      value: 5432
    - name: POSTGRES_DB
      value: postgres
    - name: POSTGRES_USER
      value: postgres
    - name: POSTGRES_PASSWORD
      value: susude00!!