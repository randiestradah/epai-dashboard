version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm install --legacy-peer-deps
      - npm run build
run:
  runtime-version: 22.12.0
  command: npm start
  network:
    port: 3001
    env: PORT
  env:
    - name: NODE_ENV
      value: production
    - name: AWS_REGION
      value: ap-southeast-3